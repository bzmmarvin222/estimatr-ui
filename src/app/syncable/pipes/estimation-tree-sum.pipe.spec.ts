import {EstimationTreeSumPipe} from './estimation-tree.pipe';

describe('EstimationTreePipe', () => {
  it('create an instance', () => {
    const pipe = new EstimationTreeSumPipe();
    expect(pipe).toBeTruthy();
  });
});
